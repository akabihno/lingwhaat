nelmio_api_doc:
  documentation:
    info:
      title: Language Detection API
      description: Language Detection API
      version: 1.0.1
    servers:
      - url: /api
        description: "%env(string:DOMAIN)%"
  areas:
    path_patterns:
      - ^/