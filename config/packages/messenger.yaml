framework:
  messenger:
    transports:
      scheduler: doctrine://default   # or enqueue/redis/etc. Avoid sync:// for recurring jobs
    routing:
      Symfony\Component\Console\Messenger\RunCommandMessage: scheduler